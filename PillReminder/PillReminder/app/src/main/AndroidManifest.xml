<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sys1.prescriptor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Regiser" />
        <activity
            android:name=".Users"
            android:label="@string/class_prompt"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".doctor_preview" />
        <activity android:name=".Details" />
        <activity android:name=".Skindoc" />
        <activity android:name=".Braindoc"></activity>
    </application>

</manifest> <!-- android:label="@string/title_activity_users" --><!--
        android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
-->
